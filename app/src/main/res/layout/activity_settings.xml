<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.wilmotbroswordpress.mckinleytaire.Settings">

    <Button
        android:id="@+id/set_menu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="Main Menu"/>
    <TextView
        android:id="@+id/volume"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/set_menu"
        android:layout_centerInParent="true"
        android:text="Volume"/>
    <SeekBar
        android:id="@+id/volbar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/volume"
        android:layout_alignEnd="@+id/volume"
        android:layout_alignRight="@+id/volume"
        android:layout_marginBottom="20dp" />
</RelativeLayout>
